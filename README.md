- yank(in the clipboard)
    - "+y
        - " (double quote)
        - + (plus sign)
        - y (yank)

- yank (a word with cursor at the beggining of the word)
    - yw
        - y(yank)
        - w()

